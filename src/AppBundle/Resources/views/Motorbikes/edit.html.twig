{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Motorbike edit</h1>
    {{ form(edit_form) }}
        <a class="btn btn-default" href="{{ path('motorbikes') }}">
            Back to the list
        </a>
    {{ form(delete_form) }}
{% endblock %}
